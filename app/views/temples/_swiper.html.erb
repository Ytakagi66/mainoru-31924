<div class="swiper-container gallery-top">
  <div class="swiper-wrapper">
    <% @temple.images.each do |image|%>
      <%= image_tag image, class: "swiper-slide" %>
    <% end %>
  </div>
<!-- If we need pagination -->
<div class="swiper-pagination"></div>
</div>