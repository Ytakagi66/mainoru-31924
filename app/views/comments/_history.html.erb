<div class="comment_preview">
  <div class = "comment_image">
  
    <div class="comment-image comment">
      <!-- 投稿したメッセージ内容を記述する -->
      <div class='title'><%= history.title %></div>
      <%= link_to history.image, 'data-lightbox': @history.image do %>
      <%= image_tag history.image.variant(resize: '600x600'), class: 'comment-image' if history.image.attached? %>
      <% end %>
        <div class = "comment_text">
          <div class="comment-user comment">
            <!-- 投稿したユーザー名情報を出力する -->
            <div class='name'><%= history.user.nickname %></div>
          </div>  
          <div class="comment-content comment">
            <!-- 投稿したメッセージ内容を記述する -->
            <%= history.text %>
          </div>
        </div>
      </div>      
    </div>
</div>
